repos:
  - repo: local
    hooks:
      - id: golangci-lint
        name: golangci-lint (backend)
        entry: powershell -Command "cd backend; golangci-lint run --config=.golangci.yml"
        language: system
        files: ^backend/.*\.go$
        types: [go]
        pass_filenames: false